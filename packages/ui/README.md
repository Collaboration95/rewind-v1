# UI package placeholder

This future package will contain Rewind tokens and reusable presentation
components. It must be an original implementation; it may use familiar mobile
feed patterns but must not copy another product's UI or assets.
