# Terraform placeholder

Future Terraform modules will provision Rewind's cloud environment after the
local prototype validates the product workflow. This folder intentionally has
no provider configuration, state backend, resources, or cloud credentials in
V1.
